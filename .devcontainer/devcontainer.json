{
	"name": "dbt Zero to Hero",
	"image": "ghcr.io/nordquant/complete-dbt-bootcamp-zero-to-hero/dbt-bootcamp-devcontainer:latest",
	"forwardPorts": [5000],
	"postCreateCommand": "uv pip install -r pyproject.toml && echo '[ -d airbnb ] && cd airbnb' >> ~/.bashrc",
	"postAttachCommand": "echo 'Welcome to the dbt Zero to Hero course!'",
	"customizations": {
		"vscode": {
			"settings": {
				"python.defaultInterpreterPath": "/usr/local/bin/python3",
				"extensions.ignoreRecommendations": true,
				"settingsSync.enabled": false,
				"python.terminal.useEnvFile": false
			}
		}
	}
}
